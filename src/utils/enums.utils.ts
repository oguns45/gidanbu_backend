export enum ENVType {
    PRODUCTION = 'production',
    STAGING = 'staging',
    DEVELOPMENT = 'development'
}

export enum AppChannel{
    WEB = 'web',
    MOBILE = 'mobile',
    DESKTOP = 'desktop',
    WATCH = 'watch'
}

export enum UserType{
    SUPER = 'superadmin',
    ADMIN = 'admin',
    BUSINESS = 'business',
    USER = 'user'
}